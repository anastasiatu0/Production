<Window x:Class="Production.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Production"
        mc:Ignorable="d"
        Title="Товары" Height="450" Width="800" Icon="/Images/icon.ico">
    <Grid>
        <DataGrid x:Name="ProdGrid" d:ItemsSource="{d:SampleData ItemCount=5}" Margin="367,98,0,0" SelectionChanged="ProdGrid_SelectionChanged">
            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding [Статус]}" Value="Non">
                            <Setter Property="Background" Value="LightBlue"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding [Статус]}" Value="Big">
                            <Setter Property="Background" Value="Green"/>
                            <Setter Property="Foreground" Value="White"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>

        </DataGrid>
        <Image HorizontalAlignment="Left" Height="73" Margin="367,0,0,0" VerticalAlignment="Top" Width="123" Source="/Images/icon.jpg"/>
        <Image x:Name="ProdPicture" HorizontalAlignment="Left" Height="284" Margin="29,73,0,0" VerticalAlignment="Top" Width="284" Source="/Images/picture.png"/>
        <TextBlock x:Name="NameBlock" HorizontalAlignment="Left" Margin="584,10,0,0" TextWrapping="Wrap" Text="TextBlock" VerticalAlignment="Top" Height="15" Width="206"/>
        <TextBlock x:Name="LoginBox" HorizontalAlignment="Left" Margin="584,26,0,0" TextWrapping="Wrap" Text="TextBlock" VerticalAlignment="Top" Height="15" Width="206" Foreground="#FFADADAD"/>
        <TextBox x:Name="SearchBox" HorizontalAlignment="Left" Margin="367,73,0,0" TextWrapping="Wrap" Text=" " VerticalAlignment="Top" Width="423" Height="20" TextChanged="RefreshGrid"/>
        <Button Content="Выйти" HorizontalAlignment="Left" Margin="734,10,0,0" VerticalAlignment="Top" Height="26" Width="56" Click="Button_Click"/>
        <Button Content="+" HorizontalAlignment="Left" Margin="367,48,0,0" VerticalAlignment="Top" x:Name="AddProductButton" Visibility="Collapsed"/>


    </Grid>
</Window>
